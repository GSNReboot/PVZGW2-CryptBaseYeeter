if (ev_mouse_enter){
selected = true;
if (buttonid = "steam dir"){
	obj_delete.tip = "STEAM DIRECTORY: Selects the default STEAM installation directory of PVZGW2."
}
else if (buttonid = "origin dir"){
	obj_delete.tip = "ORIGIN DIRECTORY: Selects the default ORIGIN installation directory of PVZGW2."
}
else if (buttonid = "custom dir"){
	obj_delete.tip = "CUSTOM DIRECTORY: Manually type the installation directory of PVZGW2."
}
else if (buttonid = "pref"){
	obj_delete.tip = "PREFERENCES: Customize the tool."
}
else if (buttonid = "quit"){
	obj_delete.tip = "EXIT: Quit the tool."
}
else if (buttonid = "dir"){
	obj_delete.tip = "DIRETORY SANDBOX: Set up the tool."
}
else if (buttonid = "stop"){
	obj_delete.tip = "STOP TARGETING: Stop targeting current directory."
}
else if (buttonid = "theme"){
	obj_delete.tip = "BACKGROUND THEME: Switch the background theme."
}
}